# Pipeline MVP

运行入口：`res://Scenes/MVP/mvp_root.tscn`

## 完整流程

主菜单 → 路线地图 → 横向探索 → 宝箱/敌人/篝火/工作台 → 构筑或战斗 → 奖励 → Boss → 通关。

## 操作

- 探索：`A/D` 或方向键移动，`E/Enter` 互动，`Esc` 返回地图。
- 构筑：先点左侧卡牌，再点空格放置；点已占格移除整张牌；可以旋转、清空、恢复默认和保存。
- 战斗：点击卡牌格消耗能量并点亮；一张卡的全部占格点亮后，在右侧发动；移动和结束回合使用底部按钮。
- 调试：`F1` 显示或隐藏游戏内右侧调试面板。

## 新增战斗卡牌

| 卡牌 | 行为 |
| --- | --- |
| 冲击枪 | 造成伤害后，将怪物向远离玩家的方向击退 2 格；到达轨道边界时停止。 |
| 瞬移鞋 | 无视中间距离，落到怪物身后 1 格；越位后玩家和怪物同时翻转朝向，并播放水平翻面 Tween 动画。 |
| 突进刺剑 | 先向怪物前进 1 格，再按移动后的距离检查刺击射程并结算伤害。 |
| 淬毒剑 | 造成直接伤害，并给怪物施加按敌方回合结算的中毒 Debuff。 |
| 净化药丸 | 清除玩家身上的全部 Debuff。 |
| 力量墨镜 | 给玩家添加限时力量 Buff；力量会增加枪、剑等所有直接伤害。 |

新开局会拥有以上卡牌各 1 张。它们不会全部塞入默认的 3×2 构筑板，需要在“整理构筑”中自行替换和组合。

## 敌人 AI

项目包含 5 个可编辑 AI 资源：

- `chaser.tres`：进入射程就攻击，否则追击。
- `sniper.tres`：过近会后退，处于远程射程时攻击。
- `poisoner.tres`：远程攻击并给玩家施加中毒。
- `skirmisher.tres`：攻击后撤退，射程外快速接近。
- `guardian_boss.tres`：按回合周期获得护盾，否则重击或推进。

战斗运行时的“敌人 AI 调试”下拉框可以即时切换 AI，并重置该敌人的生命、伤害、护盾和状态；“立即执行敌人 AI”按钮可单步观察决策。

## Inspector 调试

每个场景根节点均有导出参数，可在 Godot 右侧 Inspector 直接修改：

- `mvp_root.tscn`：启动场景、启动地图进度、是否强制新局、是否显示运行时调试面板。
- `exploration.tscn`：移动速度、互动距离、世界宽度、起始位置和颜色。
- `build.tscn`：背景、空格颜色、是否显示尺寸调试按钮。
- `battle.tscn`：能量、移动消耗、轨道长度、双方起点、点亮保留规则；在“敌人 AI”分组中可配置 AI 资源数组、Boss AI、默认调试 AI 和是否允许运行时切换。
- `reward.tscn`：宝箱/战斗金钱、奖励卡牌、是否升级构筑板。
- `Resource/MVP/cards/*.tres`：卡牌占格、颜色、效果、伤害/移动/状态数值、射程和冷却。
- `Resource/MVP/enemies/*.tres`：敌人名称、HP、伤害、初始护盾、行为类型、攻击/偏好距离、移动量、毒和周期护盾。点击 `battle.tscn` 数组中的资源即可在右侧 Inspector 编辑。

## 场景与代码

- 场景：`res://Scenes/MVP/`
- 脚本：`res://Script/MVP/`
- 卡牌配置：`res://Resource/MVP/`
- 敌人 AI 配置：`res://Resource/MVP/enemies/`
- 美术参考：`res://Assets/MVP/pipeline_ui_runtime.png`
- 自动测试：运行 `res://Scenes/MVP/smoke_test.tscn`

旧的 C# 原型仍保留在原目录；正式 MVP 入口使用独立 GDScript 场景，不依赖 .NET 运行时。
